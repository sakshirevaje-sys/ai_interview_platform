export const metadata = {
  title: "NEXTHIRE-AI Secure Exam Mode",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0, fontFamily: "Arial, sans-serif", backgroundColor: "#f5f5f5" }}>{children}</body>
    </html>
  );
}
